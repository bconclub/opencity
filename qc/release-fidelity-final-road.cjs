const {chromium}=require('C:/Users/user/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const fs=require('node:fs');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true,args:['--use-angle=swiftshader','--enable-unsafe-swiftshader']});const errors=[];try{
 const p=await browser.newPage({viewport:{width:1100,height:760},serviceWorkers:'block'});p.setDefaultTimeout(120000);p.on('pageerror',e=>errors.push(e.message));await p.route('**/local-cache.js*',r=>r.fulfill({contentType:'text/javascript',body:''}));await p.route('**/multiplayer-client.js',r=>r.fulfill({contentType:'text/javascript',body:''}));
 await p.goto('http://127.0.0.1:4173/#14.6/12.9755/77.5945/-25/55');await p.waitForFunction(()=>window.cityBootReady&&window.vidhanaStreetState?.().loaded);await p.locator('[data-ride=auto]').click();await p.getByRole('button',{name:/Vidhana Soudha area/}).click();await p.waitForFunction(()=>window.autoState?.().active&&autoState().position);await p.waitForTimeout(1500);await p.screenshot({path:'qc/release-fidelity-final-road.png'});
 const evidence=await p.evaluate(()=>({camera:{center:map.getCenter().toArray(),zoom:map.getZoom(),pitch:map.getPitch(),bearing:map.getBearing()},auto:autoState(),loadingGone:!document.querySelector('#city-loading'),flightHudHidden:document.querySelector('#flight-hud').hidden,version:document.querySelector('#build-version').textContent}));fs.writeFileSync('qc/release-fidelity-final-road.json',JSON.stringify({evidence,errors,note:'Visual-only final color-delta capture; not a frame-time benchmark.'},null,2));if(errors.length)throw Error(errors.join('\n'));console.log(JSON.stringify(evidence));
 }finally{await browser.close();}})().catch(e=>{console.error(e);process.exit(1)});
