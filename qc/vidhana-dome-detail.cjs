// Historical first-revision diagnostic, pinned to preserved attempt1 source.
// Run only after explicit GPU-slot release. Uses a review-page response adapter.
const fs=require('node:fs'),assert=require('node:assert/strict');
if(!process.argv.includes('--run-gpu')){console.log('Prepared. Reserve GPU slot, then run with --run-gpu.');process.exit(0);}
const {chromium}=require('C:/Users/user/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
let html=fs.readFileSync('qc/vidhana-architecture-review.html','utf8');
assert(html.includes("import {buildLandmarks as after} from './vidhana-architecture-candidate.js'"));
html=html.replace("import {buildLandmarks as after} from './vidhana-architecture-candidate.js'","import {buildLandmarks as after} from './vidhana-dome-candidate-attempt1.js'").replace("import {buildLandmarks as before} from '../landmarks.js'","import {buildLandmarks as before} from './vidhana-architecture-candidate.js'");
html=html.replace(/<footer>[\s\S]*?<\/footer>/,'<footer>DOME REVIEW ONLY. Main collar top39.8m is estimated. Published overall45.72m remains metadata; upper pedestal/lion emblem missing. Other six domes, columns, portico and stairs unchanged.</footer>');
html=html.replace("[155,-155,39]:[185,-205,195]","[118,-118,44]:[95,-115,76]").replace('controls.target.set(0,0,18)','controls.target.set(33.188,-32.600,28)');
html=html.replace('renderer.setPixelRatio(1)','renderer.setPixelRatio(2)').replace('[118,-118,44]:[95,-115,76]','[80,-80,39]:[67,-79,52]');
(async()=>{const report={errors:[],screenshots:[],scope:'Architecture candidate versus dome-only candidate; same scene/cameras, no runtime mutation'},browser=await chromium.launch({channel:'msedge',headless:true,args:['--use-angle=swiftshader','--enable-unsafe-swiftshader']});try{const page=await browser.newPage({viewport:{width:1400,height:900},serviceWorkers:'block'});page.on('pageerror',e=>report.errors.push(e.message));await page.route('**/qc/vidhana-architecture-review.html',r=>r.fulfill({contentType:'text/html',body:html}));for(const [suffix,local] of [['build/three.module.js','three.module.js'],['examples/jsm/controls/OrbitControls.js','OrbitControls.js']])await page.route('https://unpkg.com/three@0.169.0/'+suffix,r=>r.fulfill({contentType:'text/javascript',body:fs.readFileSync('D:/CodexTools/OSM2World/'+local)}));console.log('Dome review loading with cached Three169 modules');await page.goto('http://127.0.0.1:4173/qc/vidhana-architecture-review.html');await page.waitForFunction(()=>window.vidhanaArchitectureReviewReady,null,{timeout:60000});for(const view of ['front','aerial'])for(const mode of ['before','after']){console.log('Dome '+mode+' '+view);await page.locator('#'+view).click();await page.locator('#'+mode).click();await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));const image=`qc/vidhana-dome-attempt1-detail-${mode}-${view==='aerial'?'oblique':view}.png`;await page.screenshot({path:image});report.screenshots.push(image);}report.state=await page.evaluate(()=>vidhanaArchitectureReviewState());assert.equal(report.errors.length,0);report.status='CAPTURED_REQUIRES_VISUAL_REVIEW';}finally{await browser.close();fs.writeFileSync('qc/vidhana-dome-attempt1-detail-results.json',JSON.stringify(report,null,2)+'\n');}})().catch(e=>{console.error(e);process.exitCode=1;});
