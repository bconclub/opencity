const fs=require('node:fs'),assert=require('node:assert/strict'),{execFileSync}=require('node:child_process');
const baseline=execFileSync('git',['show','334a146:district.js'],{encoding:'utf8'});fs.writeFileSync('qc/district-winding-baseline.js',baseline);let candidate=baseline;
const oldWall="function wall(a,b,base,height,kind){",newWall="function wall(a,b,base,height,kind,flip=false){";assert(candidate.includes(oldWall));candidate=candidate.replace(oldWall,newWall);
const triangles="triangle(kind,p,q,r,[0,0],[u,0],[u,v]);triangle(kind,p,r,s,[0,0],[u,v],[0,v]);";assert(candidate.includes(triangles));candidate=candidate.replace(triangles,"if(flip){triangle(kind,p,r,q,[0,0],[u,v],[u,0]);triangle(kind,p,s,r,[0,0],[0,v],[u,v]);}else{"+triangles+"}");
const oldLoop="for(const ring of rings)for(let i=0;i<ring.length;i++){";assert(candidate.includes(oldLoop));candidate=candidate.replace(oldLoop,"for(const [ringIndex,ring]of rings.entries())for(let i=0;i<ring.length;i++){\n   const signedArea=ring.reduce((sum,a,j)=>{const b=ring[(j+1)%ring.length];return sum+a[0]*b[1]-b[0]*a[1];},0);const flip=ringIndex===0?signedArea<0:signedArea>0;");
candidate=candidate.replace('wall(a,c,base,height,kind);','wall(a,c,base,height,kind,flip);').replace('wall(a,c,height,height+.85,5);','wall(a,c,height,height+.85,5,flip);');
const facade="metalness:k===0?.18:0,side:T.DoubleSide",mapped="texture(3,colour),roughness:1,side:T.DoubleSide";assert(candidate.includes(facade)&&candidate.includes(mapped));candidate=candidate.replace(facade,"metalness:k===0?.18:0,side:T.FrontSide,shadowSide:T.DoubleSide").replace(mapped,"texture(3,colour),roughness:1,side:T.FrontSide,shadowSide:T.DoubleSide");
fs.writeFileSync('qc/district-winding-candidate.js',candidate);console.log('Prepared334a146 source and triangle/UV-preserving candidate.');
