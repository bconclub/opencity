import fs from 'node:fs';globalThis.fetch=async url=>({ok:true,json:async()=>JSON.parse(fs.readFileSync(url))});const {loadDrivingData}=await import('./driving-data.js');const {buildRoadGraph,toLocal,toLngLat,spawnRoad,roadPosition}=await import('./auto-roads.js');const g=buildRoadGraph(await loadDrivingData()),target=toLocal([77.5921,12.9792]);const e=g.edges.map((e,i)=>({i,len:e.length,c:g.connected.has(e.a),d:Math.hypot(g.nodes[e.a].p[0]-target[0],g.nodes[e.a].p[1]-target[1])})).sort((a,b)=>a.d-b.d).slice(0,8);console.log(e, toLngLat(roadPosition(g,spawnRoad(g,target))));
