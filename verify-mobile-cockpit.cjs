const {chromium}=require('C:/Users/user/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true,args:['--use-angle=swiftshader','--enable-unsafe-swiftshader']});try{const p=await b.newPage({viewport:{width:330,height:633},isMobile:true,hasTouch:true});await p.goto('https://opencity-two.vercel.app');await p.locator('.multiplayer-name-dialog input').fill('Mobile check');await p.locator('.multiplayer-name-dialog button').click();await p.locator('[data-ride=helicopter]').click({timeout:120000});await p.waitForFunction(()=>flightState?.().active);await p.waitForTimeout(1000);const state=await p.evaluate(()=>{const hud=document.querySelector('#flight-hud'),controls=document.querySelector('#flight-controls');return{version:document.querySelector('#build-version').textContent,hud:JSON.parse(JSON.stringify(hud.getBoundingClientRect())),controls:JSON.parse(JSON.stringify(controls.getBoundingClientRect())),loose:hud.querySelectorAll(':scope > .gamepad-status,:scope > .tilt-controls').length,roomOpen:document.querySelector('#multiplayer-panel').open,settings:getComputedStyle(hud.querySelector('.controller-options-body')).display};});assert.equal(state.version,'v0.0.14');assert(state.hud.height<=170);assert(state.controls.bottom<state.hud.top);assert.equal(state.loose,0);assert.equal(state.roomOpen,false);assert.equal(state.settings,'none');await p.screenshot({path:'mobile-cockpit-live.png'});console.log('PASS live330px mobile cockpit',JSON.stringify(state));}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
